String findShortestNewSequence(String sequence){
	int len= sequence.length()
	String test; 
	for(int i = 1; i<= len; i++){
		for(int j = 1; j<=4; j++){
			//for(int k = 1; k<=i; k++){
				switch(){
					case 1: test + "A"; check for substring; break; 
					case 2: test + "C"; check for substring; break;
					case 3: test + "G"; check for substring; break; 
					case 4: test + "T"; check for substring; break;
				}
			///}
				clear
		}
	}	
}
//look into sets, add all strings of l 4^n. if its true go up one. keep going untill its not length i strings